VITE_USE_PROXY=true
VITE_PROXY_URL=http://localhost:4001/api/detect

# Frontend Supabase (browser) config
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
# Optional: customize table name used by the UI
VITE_SUPABASE_TABLE=file_results# Copy this file to .env and fill in real values. Do NOT commit .env with secrets.

# Use proxy to avoid CORS and hide Gemini key in browser
VITE_USE_PROXY=true
VITE_PROXY_URL=http://localhost:4000/api/detect

# If not using proxy (not recommended), set these so browser calls Gemini directly
# Warning: Keys in Vite (VITE_*) are exposed to the browser bundle
VITE_GEMINI_API_KEY=AIzaSyAWMP71-OQ1n0zod0NGUPpETT8fKwD-PGI
VITE_GEMINI_API_ENDPOINT=https://gemini.googleapis.com/v1/models/gemini-pro-vision:predict
VITE_GEMINI_MODEL=gemini-pro-vision

# Optional: Supabase frontend access (disabled by default in UI)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
